# CRM для Адвокатського Об'єднання

Система управління юридичними справами, клієнтами, часом та фінансами для адвокатського об'єднання.

## 🚀 Технологічний стек

- **Backend:** Python 3.11+, Django 4.2+
- **Database:** PostgreSQL 14+
- **Task Queue:** Celery + Redis
- **Deployment:** Docker + Docker Compose

## 📋 Основні функції

### 1. Управління справами (Case Management)
- ✅ Процесуальна воронка (FSM) для управління стадіями справи
- ✅ Контроль процесуальних строків з автоматичними нагадуваннями
- ✅ Облік ордерів адвокатів
- ✅ Управління подіями (засідання, строки, зустрічі)
- ✅ Інтеграція з ЄДРСР для автоматичного оновлення інформації

### 2. Управління клієнтами (CRM Core)
- ✅ Картки клієнтів (фізичні та юридичні особи)
- ✅ РНОКПП (ІПН) та ЄДРПОУ
- ✅ Перевірка конфлікту інтересів
- ✅ Історія взаємодій

### 3. Облік часу (Time Tracking)
- ✅ Таймер для відстеження робочого часу в реальному часі
- ✅ Ручний облік часу
- ✅ Типи робіт з різними ставками
- ✅ Звіти по часу

### 4. Біллінг
- ✅ Генерація рахунків та актів наданих послуг
- ✅ Автоматичний розрахунок на основі обліку часу
- ✅ ПДВ (20%)
- ✅ Експорт рахунків у PDF

### 5. Документообіг
- ✅ Зберігання документів зі зв'язком зі справами
- ✅ Шаблони документів (договори, клопотання)
- ✅ Повнотекстовий пошук (PostgreSQL Full-Text Search)
- ✅ Автоматична генерація документів з шаблонів

### 6. Безпека
- ✅ Система ролей (RBAC): Адвокат, Помічник, Партнер, Адміністратор
- ✅ Аудит всіх дій (django-auditlog)
- ✅ Object-level permissions (django-guardian)
- ✅ HTTPS/TLS обов'язково для production

## 🛠️ Встановлення та запуск

### Варіант 1: Docker (рекомендовано)

1. **Клонування репозиторію:**
```bash
git clone <repository_url>
cd law_crm
```

2. **Створення .env файлу:**
```bash
cp .env.example .env
```

Відредагуйте `.env` файл, встановивши необхідні параметри:
```env
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-domain.com

DB_NAME=law_crm_db
DB_USER=law_crm_user
DB_PASSWORD=your_secure_password
DB_HOST=db
DB_PORT=5432

REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
```

3. **Запуск через Docker Compose:**
```bash
docker-compose up -d
```

4. **Створення суперкористувача:**
```bash
docker-compose exec web python manage.py createsuperuser
```

5. **Доступ до системи:**
- Web: http://localhost:8000
- Admin: http://localhost:8000/admin

### Варіант 2: Локальна установка

1. **Створення віртуального оточення:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# або
venv\Scripts\activate  # Windows
```

2. **Установка залежностей:**
```bash
pip install -r requirements.txt
```

3. **Налаштування PostgreSQL:**
Створіть базу даних:
```sql
CREATE DATABASE law_crm_db;
CREATE USER law_crm_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE law_crm_db TO law_crm_user;
```

4. **Створення .env файлу:**
```bash
cp .env.example .env
```

5. **Міграції:**
```bash
python manage.py migrate
```

6. **Створення суперкористувача:**
```bash
python manage.py createsuperuser
```

7. **Збирання статики:**
```bash
python manage.py collectstatic
```

8. **Запуск сервера розробки:**
```bash
python manage.py runserver
```

9. **Запуск Celery (в окремих терміналах):**
```bash
# Worker
celery -A law_crm worker -l info

# Beat (періодичні задачі)
celery -A law_crm beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
```

## 📊 Структура проекту

```
law_crm/
├── core/                    # Користувачі, клієнти, базові налаштування
├── case_management/         # Управління справами
├── time_tracking/           # Облік часу
├── documents/               # Документообіг
├── billing/                 # Біллінг та рахунки
├── court_integrations/      # Інтеграція з ЄДРСР та судами
├── law_crm/                 # Основні налаштування Django
├── templates/               # HTML шаблони
├── static/                  # Статичні файли
├── media/                   # Завантажені файли
├── requirements.txt         # Python залежності
├── docker-compose.yml       # Docker Compose конфігурація
├── Dockerfile              # Docker образ
└── README.md               # Документація
```

## 🔒 Ролі та права доступу

- **Адміністратор:** Повний доступ до всіх функцій
- **Партнер:** Перегляд всіх справ, клієнтів, фінансових звітів
- **Адвокат:** Доступ до призначених справ, своїх клієнтів, облік часу
- **Помічник адвоката:** Обмежений доступ до справ, куди надано дозвіл

## 🔄 Celery задачі

### Періодичні задачі (Celery Beat):

1. **Перевірка процесуальних строків** (щоденно о 9:00)
   - Надсилає нагадування адвокатам про наближення строків
   - Перевіряє майбутні події (засідання)

2. **Синхронізація з ЄДРСР** (щоденно о 22:00)
   - Автоматично оновлює інформацію по справам
   - Завантажує нові судові рішення

## 🌐 Інтеграції

### ЄДРСР (Єдиний державний реєстр судових рішень)
- Автоматичне отримання судових рішень
- Синхронізація статусів справ
- Завантаження документів

### Електронний Суд (ЄСІТС)
- Модуль підготовлений для інтеграції (в разі доступності API)

## 📝 Генерація документів

Система підтримує автоматичну генерацію документів з шаблонів:
- Договори про надання правової допомоги
- Клопотання
- Позовні заяви
- Довіреності
- Акти наданих послуг

Шаблони створюються у форматі .docx з використанням плейсхолдерів `{{variable_name}}`.

## 🔍 Повнотекстовий пошук

Система використовує PostgreSQL Full-Text Search для швидкого пошуку:
- По документах
- По справах
- По клієнтах

## 📈 Звіти

Доступні звіти:
- Облік часу (по справах, адвокатах, періодах)
- Фінансові звіти
- Статистика по справах

## 🛡️ Безпека

### Для production середовища:

1. **Змініть SECRET_KEY** у `.env`
2. **Встановіть DEBUG=False**
3. **Налаштуйте ALLOWED_HOSTS**
4. **Використовуйте HTTPS** (SSL/TLS сертифікати)
5. **Регулярні бекапи бази даних:**
```bash
docker-compose exec db pg_dump -U law_crm_user law_crm_db > backup.sql
```

6. **Налаштуйте firewall** для обмеження доступу до БД

## 🧪 Тестування

```bash
python manage.py test
```

## 📞 Підтримка

Для питань та підтримки зверніться до розробників.

## 📄 Ліцензія

[Вкажіть вашу ліцензію]

---

**Розроблено згідно з вимогами українського законодавства про адвокатську діяльність**

