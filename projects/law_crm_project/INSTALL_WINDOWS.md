# Установка Law CRM на Windows (без Docker)

## 📋 Что нужно установить:

### 1. Python 3.10 или выше

**Скачать:**
- Перейдите на https://www.python.org/downloads/
- Скачайте Python 3.10 или новее для Windows
- **ВАЖНО:** При установке поставьте галочку "Add Python to PATH"

**Проверка установки:**
```cmd
python --version
```
Должно показать: `Python 3.10.x` или выше

---

### 2. PostgreSQL 14 или выше

**Скачать:**
- Перейдите на https://www.postgresql.org/download/windows/
- Скачайте PostgreSQL 14 или новее
- Запомните пароль который укажете для пользователя `postgres`

**Проверка установки:**
```cmd
psql --version
```

---

## 🚀 Установка системы

### Шаг 1: Создание базы данных

Откройте **pgAdmin** (установился вместе с PostgreSQL) или командную строку:

```cmd
psql -U postgres
```

Введите пароль который указали при установке PostgreSQL.

Выполните команды:

```sql
-- Создаем базу данных
CREATE DATABASE law_crm_db;

-- Создаем пользователя
CREATE USER law_crm_user WITH PASSWORD 'password123';

-- Даем права
GRANT ALL PRIVILEGES ON DATABASE law_crm_db TO law_crm_user;

-- Выходим
\q
```

✅ База данных готова!

---

### Шаг 2: Настройка проекта

**1. Откройте PowerShell или CMD в папке проекта:**
```cmd
cd "D:\coursera\CBS\играюсь\срм юр"
```

**2. Создайте виртуальное окружение:**
```cmd
python -m venv venv
```

**3. Активируйте виртуальное окружение:**
```cmd
venv\Scripts\activate
```

После этого в начале строки должно появиться `(venv)`

**4. Обновите pip:**
```cmd
python -m pip install --upgrade pip
```

**5. Установите зависимости:**
```cmd
pip install -r requirements.txt
```

Это займет 2-5 минут. Дождитесь завершения.

---

### Шаг 3: Настройка .env файла

**1. Скопируйте файл настроек:**
```cmd
copy .env.example .env
```

**2. Откройте файл `.env` в блокноте и отредактируйте:**

```env
SECRET_KEY=django-insecure-change-this-key-12345
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# База данных
DB_NAME=law_crm_db
DB_USER=law_crm_user
DB_PASSWORD=password123
DB_HOST=localhost
DB_PORT=5432

# Redis (пока не используется в локальной версии)
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0

# Email (оставьте как есть)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
```

**ВАЖНО:** Если вы указали другой пароль для `law_crm_user`, замените `password123` на ваш пароль!

---

### Шаг 4: Инициализация базы данных

**1. Создайте таблицы в БД:**
```cmd
python manage.py migrate
```

Должно вывестись много строк с `OK`

**2. Создайте суперпользователя (администратора):**
```cmd
python manage.py createsuperuser
```

Введите:
- **Username:** `admin` (или любое другое имя)
- **Email:** `admin@example.com` (или ваш email)
- **Password:** введите пароль (минимум 8 символов)
- Подтвердите пароль

**3. Соберите статические файлы:**
```cmd
python manage.py collectstatic --noinput
```

---

### Шаг 5: 🎉 Запуск системы!

**1. Запустите сервер:**
```cmd
python manage.py runserver
```

**2. Откройте браузер:**
```
http://localhost:8000
```

**3. Войдите в систему:**
- Используйте username и password которые создали на Шаге 4.2

**4. Админ-панель:**
```
http://localhost:8000/admin
```

✅ **Система работает!**

---

## 📝 Загрузка демо-данных (опционально)

Чтобы протестировать систему с готовыми данными:

```cmd
python manage.py shell < scripts\create_demo_data.py
```

Тестовые пользователи:
- **Партнер:** `partner` / `partner123`
- **Адвокат:** `lawyer1` / `lawyer123`
- **Помощник:** `assistant` / `assistant123`

---

## 🔄 Как запускать в следующий раз

**1. Активируйте виртуальное окружение:**
```cmd
cd "D:\coursera\CBS\играюсь\срм юр"
venv\Scripts\activate
```

**2. Запустите сервер:**
```cmd
python manage.py runserver
```

**3. Откройте браузер:**
```
http://localhost:8000
```

---

## ⚠️ Возможные проблемы

### Проблема 1: "python не является внутренней командой"
**Решение:** Python не добавлен в PATH. Переустановите Python с галочкой "Add Python to PATH"

### Проблема 2: "psycopg2 не установлен"
**Решение:** 
```cmd
pip install psycopg2-binary
```

### Проблема 3: "No module named 'django'"
**Решение:** Убедитесь что виртуальное окружение активировано:
```cmd
venv\Scripts\activate
pip install -r requirements.txt
```

### Проблема 4: "could not connect to server: Connection refused"
**Решение:** PostgreSQL не запущен. Запустите службу:
1. Win+R → `services.msc`
2. Найдите "postgresql-x64-14"
3. Нажмите "Запустить"

### Проблема 5: "password authentication failed"
**Решение:** Неправильный пароль в `.env` файле. Проверьте `DB_PASSWORD`

---

## 📞 Нужна помощь?

1. Проверьте, что все программы установлены
2. Убедитесь что PostgreSQL запущен
3. Проверьте файл `.env` (правильный пароль БД)
4. Прочитайте сообщения об ошибках - они обычно подсказывают решение

---

## 🎯 Следующие шаги

После успешного запуска:

1. **Создайте первого клиента:**
   - Перейдите: Клиенты → Новий клієнт
   - Заполните данные

2. **Создайте дело:**
   - Перейдите: Справи → Нова справа
   - Привяжите к клиенту

3. **Учет времени:**
   - Перейдите: Облік часу → Таймер
   - Запустите таймер для дела

4. **Сгенерируйте счет:**
   - Перейдите: Рахунки → Новий рахунок
   - Система автоматически рассчитает сумму

---

## 💡 Полезные команды

```cmd
# Активация виртуального окружения
venv\Scripts\activate

# Запуск сервера
python manage.py runserver

# Создание миграций (после изменения моделей)
python manage.py makemigrations
python manage.py migrate

# Django shell (для тестирования кода)
python manage.py shell

# Создание нового суперпользователя
python manage.py createsuperuser
```

---

## 🚫 Что НЕ работает без Docker/Redis:

- ❌ Celery задачи (автоматические уведомления, синхронизация с ЕГРСР)
- ❌ Celery Beat (периодические проверки сроков)

**Все остальное работает полностью!**

Если нужны Celery задачи - установите Redis:
https://github.com/microsoftarchive/redis/releases

---

**Успешной работы! ⚖️**

