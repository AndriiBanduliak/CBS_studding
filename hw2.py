# 1
# Напишіть програму, яка запитує у користувача два слова і виводить їх розділеними комою.
word1 = input("Введите первое слово: ")
word2 = input("Введите второе слово: ")
print(f"{word1}, {word2}")

# 2
# Напишіть програму, яка запитує три цілі числа a, b і x та друкує їх добуток.
a = int(input("Введите первое целое число (a): "))
b = int(input("Введите второе целое число (b): "))
c = int(input("Введите третье целое число (c): "))
product = a * b * c
print(f"Произведение чисел: {product}")

# 3
# Напишіть програму, яка розв'язує квадратне рівняння 𝑎𝑥2 + 𝑏𝑥 + 𝑐 = 0 .  Значення a, b та c вводяться з клавіатури.
# Для знаходження кореня використовуйте оператор зведення в ступінь, а не функцію math.sqrt, щоб отримати комплексні
# числа у випадку, якщо вираз під коренем негативний.

a = float(input("Введите коэффициент a: "))
b = float(input("Введите коэффициент b: "))
c = float(input("Введите коэффициент c: "))

if a == 0:
    print("Это не квадратное уравнение, так как коэффициент 'a' равен 0.")
else:
    discriminant = (b ** 2) - 4 * (a * c)

    if discriminant >= 0:
        # Дискриминант неотрицательный, корни вещественные
        sqrt_discriminant = discriminant ** 0.5
        x1 = (-b - sqrt_discriminant) / (2 * a)
        x2 = (-b + sqrt_discriminant) / (2 * a)
        print(f"Дискриминант: {discriminant}")
        print(f"Корень x1: {x1}")
        print(f"Корень x2: {x2}")
    else:
        # Дискриминант отрицательный, корни комплексные
        # Вычисляем квадратный корень из абсолютного значения дискриминанта
        sqrt_abs_discriminant = (-discriminant) ** 0.5

        # Формируем комплексные корни: real_part + imaginary_part * j
        real_part = -b / (2 * a)
        imaginary_part = sqrt_abs_discriminant / (2 * a)

        x1_real = -b / (2 * a)
        x1_imag = -sqrt_abs_discriminant / (2 * a)  # Знак минус перед корнем

        x2_real = -b / (2 * a)
        x2_imag = sqrt_abs_discriminant / (2 * a)  # Знак плюс перед корнем

        print(f"Дискриминант: {discriminant}")

        print(f"Корень x1: {x1_real} + {x1_imag}j")
        print(f"Корень x2: {x2_real} + {x2_imag}j")

# 4
# Напишіть програму, в якій користувач вводить фразу з клавіатури, яка складається з 10 символів.
# #На екрані виведіть суму ASCII-кодів символів введеного рядка.
while True:
    phrase = input("Введите фразу из 10 символов: ")
    if len(phrase) == 10:
        break
    else:
        print("Ошибка: Введите ровно 10 символов.")

ascii_sum = 0
for char in phrase:
    ascii_sum += ord(char)  # ord() возвращает ASCII-код символа

print(f"Сумма ASCII-кодов символов: {ascii_sum}")

# 5
# Напишіть програму, яка вводить з клавіатури текст і виводить його в оберненому порядку.

text = input("Введите текст: ")
reversed_text = text[::-1]  # Срез строки с шагом -1 для реверсирования
print(f"Текст в обратном порядке: {reversed_text}")

# 6
# Напишіть програму, яка запитує користувача радіус круга і виводить його площу.

import math

radius = float(input("Введите радиус круга: "))
area = math.pi * (radius ** 2)
print(f"Площадь круга с радиусом {radius}: {area}")

# 7
# Напишіть програму, використовуючи знання про змінні типу цілих чисел, щоб підрахувати, за який час транспортний засіб
# доїде з пункту А в пункт В, якщо відомі такі дані: відстань від А до В = 700 км, швидкість автомобіля буде постійною
# та дорівнює 90 км/год. Використовуйте формулу: Час = відстань / швидкість. Створіть змінні length, куди запишіть відстань,
# velocity, куди запишіть швидкість та змінну driving_time, куди запишіть результат. Після цього використайте метод print(),
# щоб вивести в консоль результа

length = 700
velocity = 90

driving_time = length / velocity

print(f"Время в пути: {driving_time} часов")

# 8
# Створіть змінну name, age, в якій зберігатиметься ім'я та вік користувача, введене з клавіатури. Виведіть у консоль
# “My name is and I am ”, але зробіть рішення таким, щоб використовувалася конкатенація рядків (через +) та приведення
# типу числа до рядка (str(...)). Тобто, щоб якщо замінити змінні name і age, то в методі print() нічого не потрібно було
# би змінювати. Наприклад, a = 15, print(“Value =” + str(a)) і буде “Value = 15”


name = input("Введите ваше имя: ")
age_str = input("Введите ваш возраст: ")

print("My name is " + name + " and I am " + str(age_str))
