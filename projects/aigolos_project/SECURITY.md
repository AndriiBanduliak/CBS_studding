# 🔒 Безопасность AIGolos

Этот документ описывает реализованные меры безопасности в проекте AIGolos.

## ✅ Реализованные меры безопасности

### 1. Rate Limiting (Ограничение частоты запросов)

**Статус:** ✅ Реализовано

Используется встроенный механизм Django REST Framework throttling:

- **Анонимные пользователи:** 100 запросов/час
- **Аутентифицированные пользователи:** 1000 запросов/час
- **ASR endpoint:** 20 запросов/час (ресурсоемкий)
- **LLM endpoint:** 100 запросов/час
- **TTS endpoint:** 50 запросов/час

**Файлы:**
- `aigolos/settings.py` - настройки throttling
- `core/throttles.py` - кастомные throttle классы
- `asr/views.py`, `llm/views.py`, `tts/views.py` - применение throttling

### 2. Валидация типов файлов

**Статус:** ✅ Реализовано

В ASR endpoint добавлена многоуровневая валидация:

1. **Проверка расширения файла** - только разрешенные форматы
2. **Проверка MIME type** - валидация content-type
3. **Проверка magic bytes** - анализ заголовка файла для определения реального типа

**Поддерживаемые форматы:**
- WAV, MP3, FLAC, OGG, WEBM, M4A

**Файлы:**
- `core/validators.py` - функция `validate_audio_file()`
- `asr/views.py` - применение валидации

### 3. CORS настройки

**Статус:** ✅ Реализовано

- В **DEBUG режиме:** разрешены все источники (для разработки)
- В **production:** только разрешенные домены из переменной окружения `CORS_ALLOWED_ORIGINS`
- Настроены разрешенные методы и заголовки
- Включены credentials для production

**Настройка:**
```bash
# В .env файле для production
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

**Файлы:**
- `aigolos/settings.py` - настройки CORS

### 4. Health Check без аутентификации

**Статус:** ✅ Реализовано

Endpoint `/api/health/` теперь доступен без аутентификации для мониторинга:
- Возвращает статус приложения
- Включает информацию о пользователе только если он аутентифицирован

**Файлы:**
- `core/views.py` - изменен `health_view()`

### 5. Защита от XSS (Cross-Site Scripting)

**Статус:** ✅ Защищено

**Django автоматически экранирует:**
- Все переменные в шаблонах через `{{ variable }}` автоматически экранируются
- Использование `|safe` фильтра минимизировано

**JavaScript:**
- Использование `textContent` вместо `innerHTML` для безопасного вывода пользовательского контента
- Валидация и санитизация входных данных на клиенте

**Проверенные места:**
- `templates/base.html` - `{{ user.username }}` автоматически экранируется
- `static/js/dashboard.js` - использование `textContent` для вывода сообщений

### 6. Защита от SQL Injection

**Статус:** ✅ Защищено

**Django ORM автоматически параметризует запросы:**
- Все запросы используют Django ORM методы (`filter()`, `get()`, `create()`)
- Никаких raw SQL запросов с пользовательским вводом
- Все параметры автоматически экранируются

**Проверенные места:**
- `asr/views.py` - `Transcription.objects.filter(user=request.user)`
- `llm/views.py` - `Conversation.objects.filter(id=conversation_id, user=request.user)`
- `tts/views.py` - использование ORM методов

**Комментарии в коде:**
- Добавлены комментарии о защите от SQL injection в критических местах

### 7. Дополнительные заголовки безопасности

**Статус:** ✅ Реализовано (для production)

В production режиме автоматически устанавливаются:
- `X-XSS-Protection: 1; mode=block`
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `Strict-Transport-Security` (HSTS)
- `Secure` флаги для cookies

**Файлы:**
- `aigolos/settings.py` - настройки security headers

## 🔐 Рекомендации для production

### Переменные окружения

```bash
# Обязательные для production
SECRET_KEY=your-very-secure-secret-key-here
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
CORS_ALLOWED_ORIGINS=https://yourdomain.com

# Рекомендуемые
SECURE_SSL_REDIRECT=True  # Если не за reverse proxy
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
```

### Reverse Proxy

Если используете Nginx/Apache перед Django:
- Установите `SECURE_SSL_REDIRECT=False`
- Настройте SSL на уровне reverse proxy
- Используйте заголовки `X-Forwarded-Proto` и `X-Forwarded-For`

### Мониторинг

- Настройте мониторинг rate limiting (логирование превышений)
- Отслеживайте попытки загрузки невалидных файлов
- Мониторьте health check endpoint

## 📝 Проверка безопасности

### Ручная проверка

1. **Rate Limiting:**
   ```bash
   # Попробуйте отправить >20 запросов на /api/asr/transcribe/
   # Должен вернуться 429 Too Many Requests
   ```

2. **Валидация файлов:**
   ```bash
   # Попробуйте загрузить .exe файл с расширением .wav
   # Должна быть ошибка валидации
   ```

3. **Health Check:**
   ```bash
   curl http://localhost:8000/api/health/
   # Должен работать без токена
   ```

### Автоматизированная проверка

Рекомендуется использовать:
- `django-security-check` для проверки настроек
- `bandit` для статического анализа кода
- `safety` для проверки уязвимостей в зависимостях

## 🚨 Известные ограничения

1. **python-magic** - опциональная зависимость для более точного определения типов файлов
   - Может требовать установки системных библиотек (libmagic)
   - Текущая реализация использует magic bytes без python-magic

2. **Rate limiting** - использует in-memory cache по умолчанию
   - Для распределенных систем рекомендуется Redis
   - Настройка: `REST_FRAMEWORK['DEFAULT_THROTTLE_CLASSES']` с Redis backend

## 📚 Дополнительные ресурсы

- [Django Security](https://docs.djangoproject.com/en/stable/topics/security/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Django REST Framework Security](https://www.django-rest-framework.org/topics/security/)

---

**Последнее обновление:** 2025-11-08

