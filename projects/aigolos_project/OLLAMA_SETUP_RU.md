# 🚀 Инструкция по установке и запуску AIGolos с Ollama

## Шаг 1: Установка Ollama

### Windows:
1. Скачайте Ollama с [https://ollama.ai](https://ollama.ai)
2. Запустите установщик `ollama-windows-amd64.exe`
3. Следуйте инструкциям установщика

### Проверка установки:
Откройте PowerShell или Command Prompt и выполните:
```bash
ollama --version
```

Если команда работает, Ollama установлен правильно.

---

## Шаг 2: Запуск Ollama

### Автоматический запуск (рекомендуется):
Ollama обычно запускается автоматически как служба Windows после установки.

### Ручной запуск:
Если Ollama не запустился автоматически, откройте PowerShell и выполните:
```bash
ollama serve
```

**Важно:** Оставьте это окно открытым! Ollama должен работать в фоне.

### Проверка работы Ollama:
Откройте браузер и перейдите по адресу:
```
http://localhost:11434
```

Если видите ответ от Ollama, значит он работает правильно.

---

## Шаг 3: Загрузка модели

Ваше приложение использует модель `qwen2.5:7b` по умолчанию.

### Загрузите модель:
Откройте PowerShell и выполните:
```bash
ollama pull qwen2.5:7b
```

**Примечание:** Это может занять несколько минут, так как модель весит около 4-5 GB.

### Альтернативные модели (меньше размер):
Если хотите использовать более легкую модель:
```bash
# Меньше размер, быстрее работает
ollama pull qwen2.5:3b

# Или еще меньше
ollama pull qwen2.5:1.5b
```

После этого нужно будет изменить настройку в `.env` файле (см. ниже).

---

## Шаг 4: Настройка Django приложения

### 4.1. Активация виртуального окружения

Если вы еще не активировали виртуальное окружение:
```bash
# В PowerShell
.\venv\Scripts\Activate.ps1

# Если не работает, попробуйте:
venv\Scripts\activate
```

### 4.2. Создание/проверка .env файла

Создайте файл `.env` в корне проекта (если его еще нет):

```env
# Django Settings
SECRET_KEY=your-secret-key-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite по умолчанию, не требует настройки)
# DATABASE_URL=postgresql://user:password@localhost:5432/aigolos

# ASR Settings (Speech Recognition)
ASR_MODEL_NAME=large-v3
ASR_DEVICE=cpu
ASR_COMPUTE_TYPE=int8

# LLM Settings (Ollama)
LLM_BACKEND=ollama
LLM_MODEL_NAME=qwen2.5:7b
OLLAMA_BASE_URL=http://localhost:11434
LLM_MAX_TOKENS=256
LLM_TEMPERATURE=0.7

# TTS Settings (Text-to-Speech)
TTS_VOICE_NAME=de_DE/thorsten/medium
TTS_MODEL_PATH=

# Session Settings
SESSION_TIMEOUT=3600
MAX_AUDIO_SIZE=10485760
```

**Важно:** Если вы загрузили другую модель (например, `qwen2.5:3b`), измените строку:
```env
LLM_MODEL_NAME=qwen2.5:3b
```

### 4.3. Применение миграций базы данных

```bash
python manage.py migrate
```

### 4.4. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

Это нужно для доступа к админ-панели Django.

### 4.5. Сбор статических файлов

```bash
python manage.py collectstatic
```

---

## Шаг 5: Запуск приложения

### 5.1. Убедитесь, что Ollama запущен

Проверьте, что Ollama работает:
```bash
ollama list
```

Должна отобразиться загруженная модель `qwen2.5:7b`.

### 5.2. Запустите Django сервер

В PowerShell (с активированным виртуальным окружением):
```bash
python manage.py runserver
```

Вы должны увидеть:
```
Starting development server at http://127.0.0.1:8000/
```

### 5.3. Откройте приложение в браузере

Перейдите по адресу:
```
http://localhost:8000
```

или

```
http://127.0.0.1:8000
```

---

## Шаг 6: Первый запуск

1. **Зарегистрируйтесь** или **войдите** в систему
2. На главной странице вы увидите:
   - 🇩🇪 **German Speaking Trainer** - тренажер немецкого языка
   - 🎤 **Speech Recognition** - распознавание речи
   - 🤖 **AI Chat** - чат с AI
   - 🔊 **Text-to-Speech** - синтез речи

3. Попробуйте **German Speaking Trainer**:
   - Нажмите "Start Training"
   - Нажмите "Generate German Sentence"
   - Прослушайте предложение
   - Запишите свое произношение
   - Получите обратную связь от AI

---

## 🔧 Решение проблем

### Проблема: "Ollama connection failed"

**Решение:**
1. Убедитесь, что Ollama запущен:
   ```bash
   ollama serve
   ```

2. Проверьте, что модель загружена:
   ```bash
   ollama list
   ```

3. Проверьте URL в `.env` файле:
   ```env
   OLLAMA_BASE_URL=http://localhost:11434
   ```

### Проблема: "Model not found"

**Решение:**
Загрузите модель:
```bash
ollama pull qwen2.5:7b
```

### Проблема: Приложение не запускается

**Решение:**
1. Убедитесь, что виртуальное окружение активировано
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Примените миграции:
   ```bash
   python manage.py migrate
   ```

### Проблема: Порт 8000 занят

**Решение:**
Запустите на другом порту:
```bash
python manage.py runserver 8001
```

---

## 📝 Полезные команды Ollama

```bash
# Список загруженных моделей
ollama list

# Загрузить модель
ollama pull qwen2.5:7b

# Удалить модель
ollama rm qwen2.5:7b

# Запустить интерактивный чат с моделью
ollama run qwen2.5:7b

# Остановить Ollama (если запущен вручную)
# Нажмите Ctrl+C в окне, где запущен ollama serve
```

---

## 🎯 Быстрый старт (краткая версия)

```bash
# 1. Установите Ollama с https://ollama.ai

# 2. Загрузите модель
ollama pull qwen2.5:7b

# 3. Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# 4. Примените миграции
python manage.py migrate

# 5. Запустите сервер
python manage.py runserver

# 6. Откройте http://localhost:8000
```

---

## 📚 Дополнительная информация

- **Ollama документация**: https://ollama.ai/docs
- **Доступные модели**: https://ollama.ai/library
- **Проект на GitHub**: См. README.md

---

**Готово!** Теперь ваше приложение должно работать с Ollama! 🎉

