# Law CRM - Швидкий старт

## 🚀 Швидкий запуск (Windows)

### Варіант 1: Docker (найпростіший)

1. **Встановіть Docker Desktop для Windows**
   - Завантажте з https://www.docker.com/products/docker-desktop
   - Запустіть та дочекайтесь ініціалізації

2. **Запустіть setup скрипт:**
   ```cmd
   setup.bat
   ```
   Виберіть варіант 1 (Docker)

3. **Готово!** Відкрийте браузер:
   - http://localhost:8000

### Варіант 2: Локальна установка

1. **Встановіть залежності:**
   - Python 3.10+ (https://www.python.org/downloads/)
   - PostgreSQL 14+ (https://www.postgresql.org/download/windows/)

2. **Створіть базу даних:**
   ```sql
   CREATE DATABASE law_crm_db;
   CREATE USER law_crm_user WITH PASSWORD 'password';
   GRANT ALL PRIVILEGES ON DATABASE law_crm_db TO law_crm_user;
   ```

3. **Запустіть setup:**
   ```cmd
   setup.bat
   ```
   Виберіть варіант 2 (Локальна установка)

4. **Запустіть сервер:**
   ```cmd
   run.bat
   ```

## 🎯 Перші кроки

### 1. Вхід в систему
- Відкрийте http://localhost:8000
- Увійдіть з логіном та паролем суперкористувача

### 2. Створення першого клієнта
1. Натисніть "Клієнти" → "Новий клієнт"
2. Заповніть форму:
   - Тип: Фізична особа або Юридична особа
   - ПІБ / Назва компанії
   - РНОКПП (ІПН) або ЄДРПОУ
   - Контактні дані

### 3. Створення справи
1. Натисніть "Справи" → "Нова справа"
2. Заповніть:
   - Номер справи (напр. 910/1234/24)
   - Назва справи
   - Клієнт (виберіть зі списку)
   - Тип провадження
   - Суд

### 4. Облік часу
1. Перейдіть до "Облік часу" → "Таймер"
2. Виберіть справу та тип роботи
3. Натисніть "Старт"
4. По завершенні натисніть "Стоп"

### 5. Генерація рахунку
1. Перейдіть до "Рахунки" → "Новий рахунок"
2. Виберіть справу та клієнта
3. Система автоматично розрахує суму на основі обліку часу
4. Експортуйте рахунок у PDF

## 📖 Демонстраційні дані

Для завантаження демонстраційних даних:

**Docker:**
```cmd
docker-compose exec web python manage.py shell < scripts/create_demo_data.py
```

**Локально:**
```cmd
venv\Scripts\activate
python manage.py shell < scripts\create_demo_data.py
```

Тестові користувачі:
- **Партнер:** `partner` / `partner123`
- **Адвокат:** `lawyer1` / `lawyer123`
- **Помічник:** `assistant` / `assistant123`

## 🔧 Корисні команди

### Docker

```cmd
# Запустити систему
docker-compose up -d

# Зупинити систему
docker-compose down

# Переглянути логи
docker-compose logs -f web

# Виконати міграції
docker-compose exec web python manage.py migrate

# Створити суперкористувача
docker-compose exec web python manage.py createsuperuser

# Django shell
docker-compose exec web python manage.py shell

# Перезапустити контейнери
docker-compose restart
```

### Локально

```cmd
# Активувати venv
venv\Scripts\activate

# Запустити сервер
python manage.py runserver

# Міграції
python manage.py migrate

# Celery worker
celery -A law_crm worker -l info

# Celery beat
celery -A law_crm beat -l info
```

## 📱 Основні розділи системи

### 🏠 Головна (Dashboard)
- Статистика по справам
- Останні справи
- Швидкий доступ

### 👥 Клієнти
- Список клієнтів
- Фізичні та юридичні особи
- РНОКПП / ЄДРПОУ
- Перевірка конфлікту інтересів

### 💼 Справи
- Управління справами
- Процесуальна воронка (FSM)
- Контроль строків
- Ордери адвокатів
- Події та засідання

### ⏱️ Облік часу
- Таймер в реальному часі
- Ручний облік
- Типи робіт
- Звіти по часу

### 📄 Документи
- Завантаження документів
- Шаблони документів
- Автогенерація
- Повнотекстовий пошук

### 💰 Рахунки (Біллінг)
- Генерація рахунків
- Акти наданих послуг
- Облік платежів
- Експорт у PDF

### ⚖️ Адміністрування
- Управління користувачами
- Налаштування системи
- Журнал аудиту
- Інтеграції

## 🔐 Ролі та права

| Роль | Права |
|------|-------|
| **Адміністратор** | Повний доступ до всіх функцій |
| **Партнер** | Перегляд всіх справ, фінансові звіти |
| **Адвокат** | Власні справи, клієнти, облік часу |
| **Помічник** | Обмежений доступ до призначених справ |

## 🆘 Вирішення проблем

### Помилка підключення до БД
```
Переконайтеся що:
1. PostgreSQL запущено
2. База даних створена
3. Параметри в .env правильні
```

### Docker не запускається
```
1. Перевірте чи запущено Docker Desktop
2. Перевірте чи достатньо пам'яті
3. docker-compose down && docker-compose up -d
```

### Celery не працює
```
1. Перевірте чи запущено Redis
2. Перевірте параметри CELERY_BROKER_URL в .env
```

## 📚 Додаткова документація

- Повна документація: `README.md`
- Технічне завдання: файл з ТЗ
- API документація: `/api/docs` (якщо увімкнено)

## 🔗 Корисні посилання

- **Django документація:** https://docs.djangoproject.com/
- **Celery документація:** https://docs.celeryproject.org/
- **PostgreSQL документація:** https://www.postgresql.org/docs/

## 💡 Поради

1. **Регулярно робіть бекапи БД:**
   ```cmd
   docker-compose exec db pg_dump -U law_crm_user law_crm_db > backup.sql
   ```

2. **Використовуйте HTTPS у production**

3. **Налаштуйте email для сповіщень**

4. **Регулярно оновлюйте залежності**

## 📞 Підтримка

При виникненні питань:
1. Перевірте документацію
2. Перегляньте логи системи
3. Зверніться до розробників

---

**Успішної роботи з Law CRM! ⚖️**

